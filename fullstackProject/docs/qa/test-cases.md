# Тест-кейсы: CineBooking

> **Где живут кейсы в индустрии.** Живой источник — этот md в репозитории
> (docs-as-code, ревью через PR). Снапшоты для передачи/импорта:
> [test-cases.txt](test-cases.txt) — читаемая «распечатка», как выгрузка из
> TMS; [test-cases.csv](test-cases.csv) — формат импорта в TestRail / Test IT /
> Allure TestOps (UTF-8 c BOM, открывается в Excel). В самих TMS кейсы
> хранятся в БД с прогонами и историей; Excel/Sheets — типичный вариант
> малых команд; Word — только формальные артефакты (ПМИ по ГОСТ 34).

Формат каждого кейса: идентификатор, приоритет, тип, привязка к автоматизации
(если кейс покрыт автотестом — ссылка на реальный файл в репозитории),
предусловия, шаги, ожидаемый результат, постусловия.

Приоритеты: **High** — блокирует релиз при падении; **Medium** — важная
функциональность, фикс в текущей итерации; **Low** — косметика/редкие ветки.
Типы: `функц.` / `негатив.` / `гранич.` / `конкур.` / `отказоуст.`.

Сводка: 55 кейсов, из них 53 покрыты автотестами (TC-SSE-003 — частично),
2 — ручные (нужен стенд).

---

## Модуль «Каталог фильмов»

### TC-MOV-001 — Каталог отдаёт сеансы с меткой источника
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/test/http.int.spec.ts` → «первый вызов — из „БД“», «повторный — из Redis-кэша» |
| Предусловия | Стенд поднят, таблица фильмов пуста (первый старт сеет 6 записей) |
| Шаги | 1. Открыть `GET /api/movies`. 2. Повторить запрос в течение 60 c. 3. Повторить после 60 c. |
| Ожидаемый результат | 1. `source: "db"`, 6 фильмов, у каждого `title`, `priceRub`, `sessionAt`. 2. `source: "cache"` (в Redis ключ `movies:all`). 3. Снова `source: "db"` — TTL истёк |
| Постусловия | — |

### TC-MOV-002 — Карта зала отражает занятость
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/test/http.int.spec.ts` → «GET /api/movies/:id/seats — геометрия зала…» |
| Предусловия | Есть хотя бы одна бронь на фильм |
| Шаги | 1. `GET /api/movies/:id/seats`. 2. Сверить `occupied` с местами броней из `GET /api/bookings`. 3. Проверить арифметику `free` |
| Ожидаемый результат | `layout = {rows: 8, seatsPerRow: 10}`; `occupied` содержит все места активных броней; `free = 80 − occupied.length`; повторный запрос не берётся из кэша (всегда свежие данные) |
| Постусловия | — |

---

## Модуль «Создание брони»

### TC-BKG-001 — Успешное создание брони (happy path)
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/test/http.int.spec.ts` → «создаёт PENDING-бронь…»; e2e «полный цикл» |
| Предусловия | Фильм с ценой `priceRub` существует, места `["5-7","5-8"]` свободны |
| Шаги | 1. `POST /api/bookings {movieId, customerName: "Дмитрий", seats: ["5-7","5-8"]}`. 2. Проверить ответ. 3. Проверить `GET /api/bookings` |
| Ожидаемый результат | 201; статус `PENDING`; `seats = ["5-7","5-8"]`; `totalRub = priceRub × 2`; в списке бронь видна; в RabbitMQ опубликовано `booking.created` с теми же полями |
| Постусловия | Места `5-7`, `5-8` заняты этой бронью до вердикта воркера |

### TC-BKG-002 — Валидация payload: невалидные места и поля
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / негатив., гранич. |
| Автотест | `apps/api/test/http.int.spec.ts` → параметризованный «400 при невалидных данных» |
| Предусловия | — |
| Шаги | Отправить `POST /api/bookings` с каждым из вариантов: место `["5"]`, `["9-1"]`, `["1-11"]`, `["0-1"]`, `["-1-2"]`; пустой `seats`; `seats: 2`; 9 мест; имя `"Д"`; `movieId: "abc"` |
| Ожидаемый результат | Каждый запрос → 400 с описанием ошибки ValidationPipe; ни одна бронь не создаётся, места не занимаются |
| Постусловия | — |

### TC-BKG-003 — Бронь несуществующего фильма
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / негатив. |
| Автотест | `apps/api/test/http.int.spec.ts` → «404 на несуществующий фильм» |
| Шаги | `POST /api/bookings` с корректным по формату uuid, которого нет в БД |
| Ожидаемый результат | 404, сообщение «Фильм не найден» |
| Постусловия | — |

### TC-BKG-004 — Гонка за одно место
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / конкурент. |
| Автотест | `apps/api/test/http.int.spec.ts` → «409 со списком мест…»; web: demoEngine «занятое место — ApiError 409» |
| Предусловия | Место `4-4` свободно |
| Шаги | 1. Клиент А бронирует `4-4` → 201. 2. Клиент Б бронирует `["4-3","4-4"]`. 3. Проверить карту зала |
| Ожидаемый результат | Запрос Б → 409, `seatsTaken: ["4-4"]`, сообщение содержит `4-4`; место `4-3` из отклонённого запроса **не** занялось; арбитр — уникальный констрейнт `(movie_id, seat)`, транзакция отката целиком |
| Постусловия | За местом `4-4` — бронь клиента А |

### TC-BKG-005 — Вердикт воркера: FAILED освобождает места
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/test/http.int.spec.ts` → «FAILED → места освобождаются…»; e2e — параметр `SUCCESS_RATE=0` |
| Предусловия | Создана бронь в `PENDING` |
| Шаги | 1. Дождаться вердикта (≤ 3 c). 2. Проверить статус и сообщение. 3. Попробовать забронировать те же места снова |
| Ожидаемый результат | Статус `FAILED`, сообщение про отказ банка, `processedBy = go-worker-1`; места свободны — повторная бронь тех же мест проходит (201) |
| Постусловия | Новая бронь занимает места |

---

## Модуль «Отмена брони (сага)»

### TC-CAN-001 — Успешная отмена: возврат прошёл
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/test/http.int.spec.ts` → «cancel: CONFIRMED → CANCELLING…», «booking.refunded CANCELLED → места свободны…»; e2e «сага отмены» |
| Предусловия | Бронь в `CONFIRMED`, места заняты |
| Шаги | 1. `POST /api/bookings/:id/cancel`. 2. Сразу проверить статус и карту зала. 3. Дождаться вердикта (≤ 2 c). 4. Проверить статус, карту, статистику |
| Ожидаемый результат | 1. 200, статус `CANCELLING`, опубликовано `booking.cancelled`. 2. Места **ещё заняты** (держатся до вердикта). 3–4. Статус `CANCELLED`, сообщение «Возврат … ₽ зачислен», места снова в продаже, `stats.CANCELLED` увеличился |
| Постусловия | Места можно бронировать заново |

### TC-CAN-002 — Двойной клик «Отменить»
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / конкурент. |
| Автотест | `apps/api/test/http.int.spec.ts` → «повторная отмена по CANCELLING — 409» |
| Предусловия | Бронь в `CONFIRMED` |
| Шаги | 1. Отправить два `POST /bookings/:id/cancel` подряд. |
| Ожидаемый результат | Первый → 200 (`CANCELLING`); второй → 409 с текущим статусом `CANCELLING`; второе событие `booking.cancelled` не публикуется; в UI кнопка на время запроса дизейблится |
| Постусловия | Одна сага в работе |

### TC-CAN-003 — Отмена брони не в статусе CONFIRMED
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / негатив. |
| Автотест | `apps/api/test/http.int.spec.ts` → «409 на отмену брони не в CONFIRMED (PENDING)» |
| Шаги | 1. Создать бронь (PENDING) и попытаться отменить. 2. Отменить уже `CANCELLED` бронь |
| Ожидаемый результат | Оба запроса → 409; в теле — текущий статус; в UI кнопки «Отменить» у таких броней нет |
| Постусловия | — |

### TC-CAN-004 — Отмена несуществующей брони
| Поле | Значение |
|---|---|
| Приоритет / Тип | Low / негатив. |
| Автотест | `apps/api/test/http.int.spec.ts` → «404 на отмену несуществующей брони» |
| Шаги | `POST /api/bookings/00000000-0000-0000-0000-000000000000/cancel` |
| Ожидаемый результат | 404 |
| Постусловия | — |

### TC-CAN-005 — Банк отказал в возврате: откат в CONFIRMED
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/test/http.int.spec.ts` → «booking.refunded REFUND_FAILED → откат в CONFIRMED…»; web: demoEngine «REFUND_FAILED откатывает…» |
| Предусловия | Бронь в `CANCELLING` (параметр стенда `REFUND_SUCCESS_RATE=0` гарантирует отказ) |
| Шаги | 1. Дождаться `booking.refunded` со статусом `REFUND_FAILED`. 2. Проверить статус брони, сообщение, карту зала |
| Ожидаемый результат | Бронь вернулась в `CONFIRMED`; сообщение про отказ банка; места остаются заняты; в UI у брони снова появилась кнопка «Отменить» (повторная попытка возможна) |
| Постусловия | — |

### TC-CAN-006 — Идемпотентность consumer'а при ределивери
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / отказоуст. |
| Автотест | `apps/api/test/http.int.spec.ts` → «ределивери booking.refunded по закрытой саге…»; unit: «пропущенный ределивери → без события» |
| Предусловия | Бронь уже `CANCELLED`, места освобождены |
| Шаги | 1. Повторно вызвать обработку `booking.refunded` (как при ретрае брокера) для той же брони |
| Ожидаемый результат | Событие пропущено без изменений в БД и без повторного SSE; места остаются свободными; лог содержит предупреждение о пропуске |
| Постусловия | — |

---

## Модуль «SSE-стрим»

### TC-SSE-001 — Событие booking при мутациях
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/test/http.int.spec.ts` → «SSE: GET /api/bookings/stream» (реальный HTTP-сокет, парсинг фреймов) |
| Предусловия | Открыт `GET /api/bookings/stream` (curl/DevTools) |
| Шаги | 1. Создать бронь. 2. Прочитать следующий фрейм `event: booking`. 3. Повторить для отмены и вердиктов воркера |
| Ожидаемый результат | `Content-Type: text/event-stream`; на каждую мутацию — фрейм `booking`, в `data` — JSON `{booking, stats}`; статистика в событии свежая |
| Постусловия | — |

### TC-SSE-002 — Вердикт воркера приходит без опроса
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/test/live.e2e.spec.ts` → «SSE: вердикт воркера приходит в стрим без опроса» |
| Предусловия | Стенд поднят, стрим открыт |
| Шаги | 1. Создать бронь. 2. Дождаться в стриме события с её `PENDING`. 3. Не опрашивая `/bookings`, дождаться следующего события по той же брони |
| Ожидаемый результат | Второе событие несёт `CONFIRMED`/`FAILED`, `processedBy = go-worker-1`, обновлённую статистику |
| Постусловия | — |

### TC-SSE-003 — Heartbeat и переподключение
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / отказоуст. |
| Автотест | unit: `bookings.controller.spec.ts` → «heartbeat „ping“ каждые 25 c»; web: live-тест сторa с FakeEventSource (upsert + close) |
| Шаги | 1. Держать стрим открытым > 25 c — убедиться в `ping`. 2. ✋ Перезапустить api-контейнер. 3. Наблюдать страницу «Мои брони» |
| Ожидаемый результат | `ping` каждые 25 c (клиент его игнорирует); при обрыве EventSource сам переподключается; по `onopen` фронт делает полный resync (`GET /bookings` + `stats`); ручной шаг — список актуален без F5 |
| Постусловия | — |

### TC-SSE-004 — SSE через nginx не буферизуется
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / отказоуст. (окружение) |
| Автотест | нет — только живой прокси (ручная) |
| Предусловия | Открыта страница через `http://localhost:18080` (docker-web) |
| Шаги | 1. Открыть «Мои брони». 2. Создать бронь из второй вкладки |
| Ожидаемый результат | Новая бронь и смены статусов появляются в первой вкладке практически мгновенно: `proxy_buffering off` в `apps/web/nginx.conf` не даёт событиям осесть в буфере прокси. Если события приходят пачкой с задержкой — регресс конфига |
| Постусловия | — |

---

## Модуль «Демо-режим»

### TC-DMO-001 — Паритет демо-режима с живым стеком
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / функц. |
| Автотест | `apps/web/src/api/demoEngine.spec.ts` — все кейсы движка (тайминги через fake timers, вероятности через мок Math.random) |
| Предусловия | Открыт https://dimas1barabas.github.io/CineBooking/ при недоступном API |
| Шаги | 1. Убедиться в баннере «демо-режим». 2. Пройти цикл бронь → вердикт → отмена |
| Ожидаемый результат | Тайминги (1,2–2,8 c оплата; 0,8–1,6 c возврат) и вероятности (~90%) совпадают с Go-воркером; конфликт мест даёт тот же 409-текст; сага отмены ведет себя как в live |
| Постусловия | — |

---

## Модуль «Отзывы и рейтинги»

### TC-REV-001 — Отзыв без подтверждённой брони
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / негативный |
| Автотест | int: `reviews.int.spec.ts` → «без подтверждённой брони — 403»; e2e: `live.e2e.spec.ts` → «отзыв без подтверждённой брони — 403» |
| Предусловия | Зарегистрирован пользователь без броней на фильм |
| Шаги | 1. `POST /api/movies/:id/reviews {rating: 5, text ≥ 10 симв.}` |
| Ожидаемый результат | 403, сообщение «…подтверждённая бронь»; в UI форма отзыва скрыта, показана подсказка |
| Постусловия | — |

### TC-REV-002 — Дубль отзыва (гонка)
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / конкурентный |
| Автотест | unit: `reviews.service.spec.ts` → «дубль (23505)»; int: «дубль — 409 reviewExists»; e2e: полный цикл |
| Предусловия | Пользователь уже оставил отзыв на фильм |
| Шаги | 1. Повторить `POST /api/movies/:id/reviews` |
| Ожидаемый результат | 409 с `code: reviewExists`; арбитр — uq-констрейнт `(user_id, movie_id)`; агрегат не задваивается |
| Постусловия | — |

### TC-REV-003 — Валидация отзыва
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / граничный |
| Автотест | int: «кривое тело — 400»; web: `demoEngine.spec.ts` → «валидация — 400» |
| Шаги | 1. POST с `rating: 6`, `rating: 0`, без rating. 2. POST с текстом короче 10 и длиннее 1000 символов |
| Ожидаемый результат | Каждый запрос → 400 (ValidationPipe, сообщения на русском); в UI сабмит без звёзд заблокирован |
| Постусловия | — |

### TC-REV-004 — Отзыв после брони и рейтинг в каталоге
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функциональный |
| Автотест | int: «201 после подтверждённой брони; рейтинг в каталоге»; e2e: полный цикл |
| Предусловия | У пользователя есть CONFIRMED-бронь на фильм |
| Шаги | 1. `POST /api/movies/:id/reviews`. 2. `GET /api/movies` — посмотреть фильм |
| Ожидаемый результат | 201, `authorName` из профиля; отзыв в публичном списке `GET /movies/:id/reviews`; `ratingCount`/`ratingAvg` фильма выросли сразу — каталог покинул кэш |
| Постусловия | — |

### TC-REV-005 — Удаление отзыва: своё / чужое / админом
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / негативный |
| Автотест | unit: «чужой — 403», «админ удаляет чужой»; int: DELETE-кейсы; web: `ReviewModal.spec.ts` |
| Шаги | 1. Автор удаляет свой отзыв. 2. Другой пользователь пытается удалить его. 3. Админ удаляет чужой |
| Ожидаемый результат | 1) 204, агрегат пересчитан, кэш сброшен. 2) 403 «Это не ваш отзыв». 3) 204. Поиск отзыва скоуплен по фильму — id чужого фильма не даёт доступ |
| Постусловия | — |

### TC-REV-006 — Рейтинг на карточках и демо-паритет
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / UI |
| Автотест | web: `components.spec.ts` (звёзды/каунт/«Отзывов ещё нет»), `demoEngine.spec.ts` (агрегаты, 403/409, reset) |
| Предусловия | Открыта афиша |
| Шаги | 1. Сравнить звёзды/среднюю на карточке с отзывами фильма. 2. Открыть фильм без отзывов. 3. В демо-режиме: бронь → оплата → CONFIRMED → написать отзыв |
| Ожидаемый результат | Заполненные звёзды = round(avg), рядом средняя и счётчик; без отзывов — «Отзывов ещё нет» (ряд кликабелен); в демо право на отзыв появляется после CONFIRMED-брони, дубль даёт 409, агрегат растёт |
| Постусловия | — |

---

## Модуль «Админ-аналитика»

### TC-STA-001 — Доступ к аналитике: только роль admin
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / негатив. |
| Автотест | `apps/api/test/admin.int.spec.ts` → «401 без токена», «403 с токеном обычного пользователя»; e2e: тот же контракт на живом стенде |
| Предусловия | Зарегистрирован обычный пользователь; админ сеется при старте (`admin@cine.local`) |
| Шаги | 1. `GET /api/admin/stats` без токена. 2. С токеном обычного пользователя. 3. С админским токеном |
| Ожидаемый результат | 1. 401 (deny by default). 2. 403 (RolesGuard). 3. 200 с конвертом `{source: "db"\|"cache", data}`; в Swagger маршрут за bearer-security и документирует 403 |
| Постусловия | — |

### TC-STA-002 — Сводка KPI и счётчики статусов согласованы с данными
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | unit: `admin-stats.logic.spec.ts`, `admin-stats.service.spec.ts`; int: `admin.int.spec.ts` → «200 админу»; e2e: сумма счётчиков = `bookingsTotal` на живом Postgres |
| Предусловия | В БД есть брони в разных статусах |
| Шаги | 1. `GET /api/admin/stats` под админом. 2. Сверить `totals` и `byStatus` с `GET /api/bookings`. 3. Проверить топ фильмов и ближайшие сеансы |
| Ожидаемый результат | `revenueRub/avgTicketRub/seatsSold` — только по CONFIRMED; все 7 статусов с нулями; сумма `byStatus` = `bookingsTotal`; топ-5 по подтверждённым броням (ничья — по выручке); у сеансов `capacity = 80`, прошедшие не попадают |
| Постусловия | — |

### TC-STA-003 — График выручки: окно 14 дней, день вердикта
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / гранич. |
| Автотест | unit: `admin-stats.logic.spec.ts` (api) + `utils/adminStats.spec.ts` (web); int: фикстура «вчера + сегодня»; e2e: 14 дней подряд |
| Предусловия | Есть CONFIRMED-брони с `processed_at` в разные дни |
| Шаги | 1. Создать бронь сегодня, оплатить, дождаться вердикта. 2. `GET /api/admin/stats` → `revenueByDay`. 3. Сверить нулевые дни |
| Ожидаемый результат | Ровно 14 записей от «сегодня−13» до «сегодня», по возрастанию, пустые дни — нули; бронь считается в дне вердикта (`processed_at`), не создания; не-CONFIRMED не попадают; дни — локальные `YYYY-MM-DD`, полуночная продажа не уезжает во вчера |
| Постусловия | — |

### TC-STA-004 — Кэш агрегатов: TTL 30 c, конверт source
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / функц. |
| Автотест | unit: `admin-stats.service.spec.ts` (db→cache без повторной выгрузки); int: «повторный вызов — из Redis-кэша»; e2e: `source=cache` на повторе |
| Шаги | 1. `GET /api/admin/stats` дважды подряд. 3. Подождать 30 c, повторить |
| Ожидаемый результат | Первый — `source: "db"` (ключ `admin:stats:v1` в Redis), повторный — `source: "cache"` без похода в Postgres; после TTL — снова `db`; на фронте бейдж «из кэша» |
| Постусловия | — |

### TC-STA-005 — Демо-паритет: сиды, топ, кэш, доступ без прав
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / UI |
| Автотест | web: `demoEngine.spec.ts` (сходство сводки с сидами, кэш 30 c, reset), `AdminStatsView.spec.ts` (доступ, плитки, пороги цвета, демо-пометка) |
| Предусловия | Открыт GitHub Pages-демо при недоступном API |
| Шаги | 1. Открыть `/#/admin/stats` без входа. 2. Сверить плитки с сидами «других зрителей». 3. Нажать «Обновить» дважды |
| Ожидаемый результат | Дашборд открыт всем (витрина), пометка «демо-данные»; сводка/топ/график считаются из сидов движка; повторный вызов — бейдж «из кэша» (30 c); CONFIRMED-сиды держат места в картах сеансов, но права на отзыв не дают |
| Постусловия | — |

---

## Модуль «Инфраструктура»

### TC-INF-001 — Health-check отражает состояние зависимостей
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / отказоуст. |
| Автотест | unit: `health.controller.spec.ts`; int: «все фейки живы → ok» |
| Шаги | 1. `GET /api/health` на целом стенде. 2. ✋ Остановить redis-контейнер, повторить |
| Ожидаемый результат | 1. `status: "ok"`, `checks: {postgres: up, redis: up, rabbitmq: up}`. 2. `status: "degraded"`, `redis: down` |
| Постусловия | Поднять redis обратно |

### TC-INF-002 — Воркер переживает рестарт брокера
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / отказоуст. |
| Автотест | нет — ручная на стенде |
| Шаги | 1. ✋ `docker compose restart rabbitmq`. 2. Смотреть логи воркера. 3. Создать бронь после восстановления |
| Ожидаемый результат | Воркер пишет «попытка N …» с бэкоффом ~3 c, переподключается и обрабатывает новые брони; сообщения, опубликованные в простой, не теряются (durable-очереди) |
| Постусловия | — |

---

## Модуль «Аккаунт и сессии»

### TC-ACC-001 — Refresh-ротация продлевает сессию без повторного входа
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | `apps/api/src/tokens/tokens.service.spec.ts`; int: «ротация — новая пара и новая кука»; e2e «refresh продлевает сессию»; web: `client.spec.ts` → «401 → один refresh → повтор» |
| Предусловия | Пользователь вошёл; access (TTL 2 ч) протух, refresh-cookie жива (30 дней) |
| Шаги | 1. `POST /api/auth/refresh` с cookie `cine.refresh`. 2. Проверить ответ и новую куку. 3. Дёрнуть авторизованный маршрут новым access |
| Ожидаемый результат | 200: `{accessToken, user}` (без refreshToken в теле); кука ротирована (новое значение, старая строка `revoked_at`); новый access работает. Фронт повторяет упавший на 401 запрос сам, один refresh на пачку |
| Постусловия | — |

### TC-ACC-002 — Переиспользование отозванного refresh ревокает все сессии
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / конкуренция |
| Автотест | unit: «reuse → 401 и ВСЕ сессии»; int: «переиспользование ротированной куки убивает и свежую сессию»; e2e аналогично |
| Предусловия | У пользователя ≥ 2 refresh-сессий (например, два устройства) |
| Шаги | 1. Ротировать cookie-токен устройства A. 2. Использовать его повторно (старая копия). 3. Попробовать свежую куку устройства B |
| Ожидаемый результат | Шаг 2 — 401; reuse расценивается как компрометация: все сессии пользователя отозваны (шаг 3 — тоже 401). Гонка параллельных refresh решается условным UPDATE (`revoked_at IS NULL`, affected=0 → тот же исход) |
| Постусловия | Пользователь должен войти заново |

### TC-ACC-003 — Logout гасит сессию и снимает куку
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | int: «204: гасит сессию — refresh после logout отклоняется»; web: store.spec → «logout при недоступном API всё равно выходит локально» |
| Предусловия | Пользователь вошёл (Bearer + cookie) |
| Шаги | 1. `POST /api/auth/logout` с Bearer и cookie. 2. `POST /api/auth/refresh` снятой кукой. 3. На фронте нажать «Выйти» |
| Ожидаемый результат | 204, `Set-Cookie: cine.refresh=;` (снятие); refresh отклонён; фронт чистит локальную сессию даже при недоступном API и уходит с /profile на витрину |
| Постусловия | — |

### TC-ACC-004 — Профиль: имя/email со свежей JWT-парой в ответе
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | unit: users.service.spec → updateProfile; int: `users.int.spec.ts`; e2e «PATCH /users/me» |
| Предусловия | Пользователь вошёл |
| Шаги | 1. `PATCH /api/users/me {name}`. 2. Раскрыть access из ответа. 3. `PATCH {email}` на занятый адрес. 4. `PATCH {}` |
| Ожидаемый результат | 1. 200, `user.name` обновлён, access-клеймы несут новое имя (свежая пара — старый JWT протух бы со старым). 2. Новый access проходит на `/bookings/my`. 3. 409 `emailTaken`. 4. 400 «нечего обновлять». Без токена — 401 |
| Постусловия | — |

### TC-ACC-005 — Смена пароля: выход отовсюду, это устройство остаётся
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | int: `users.int.spec.ts` → «хэш заменён, все прежние сессии отозваны»; e2e «смена пароля: все сессии отозваны, вход — только с новым» |
| Предусловия | Две сессии: это устройство (текущий вход) и «другое устройство» (отдельная cookie) |
| Шаги | 1. `PUT /api/users/me/password {currentPassword, newPassword}`. 2. `POST /api/auth/refresh` кукой «другого устройства». 3. Логин старым паролем. 4. Логин новым. 5. Повторить PUT с неверным текущим |
| Ожидаемый результат | 1. 200: LoginResult + свежая cookie этому устройству (перелогин не нужен), пароль в БД перехэширован. 2. 401 — все прежние сессии отозваны. 3. 401. 4. 200. 5. 403 «Неверный текущий пароль» |
| Постусловия | — |

### TC-ACC-006 — Запрос сброса не раскрывает существование email
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / негатив. |
| Автотест | unit: «неизвестный email: тишина»; int: «200 для выдуманного — ответ неотличим»; e2e «forgot без оракула» |
| Предусловия | Стенд поднят |
| Шаги | 1. `POST /api/auth/forgot-password` для существующего email. 2. То же для выдуманного. 3. Сравнить ответы |
| Ожидаемый результат | Оба 200, тела неотличимы; событие `user.password.reset` опубликовано только для существующего. Кривой email — 400 |
| Постусловия | — |

### TC-ACC-007 — Полный цикл восстановления по ссылке из «письма»
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | e2e «полный цикл: forgot → письмо notification-service → reset → вход новым» (graceful-skip без стенда); int: reset 200 |
| Предусловия | Пользователь существует; notification-service поднят |
| Шаги | 1. `POST /api/auth/forgot-password`. 2. Взять ссылку из «письма» (`:18082/notifications`, kind `password_reset`). 3. Открыть `/reset-password?token=…`, задать новый пароль. 4. Войти старым и новым паролем |
| Ожидаемый результат | «Письмо» печатается и сохраняется в истории notification-service (адресат — email); reset 200 — это устройство сразу залогинено (пара + cookie); старый пароль — 401, новый — 200 |
| Постусловия | — |

### TC-ACC-008 — Ссылка сброса одноразовая и живёт 30 минут
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / гранич. |
| Автотест | unit: «повторное использование ссылки → 400», «протухшая ссылка → 400»; int: «мусорный токен» |
| Предусловия | Есть использованная ссылка и ссылка с истёкшим TTL |
| Шаги | 1. `POST /api/auth/reset-password` с уже использованным токеном. 2. С протухшим (older 30 мин). 3. С мусорной строкой |
| Ожидаемый результат | Все три — 400 с одинаковым текстом «Ссылка недействительна или истекла» (не раскрываем, какая именно причина). Одноразовость атомарна: условный UPDATE по `used_at IS NULL` закрывает гонку двух кликов |
| Постусловия | — |

---

## Модуль «Промокоды и скидки»

### TC-PRM-001 — Админ создаёт промокод, код нормализуется
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | unit: `promos.service.spec` → «нормализует код в верхний регистр»; int: «201 админу: код нормализован в верхний регистр»; e2e «админ создаёт промокод…»; web: AdminView.spec «создание промокода — код нормализован» |
| Предусловия | Админ авторизован |
| Шаги | 1. `POST /api/promos` с кодом в нижнем регистре и пробелами по краям поля (внутри — без). 2. `GET /api/promos` |
| Ожидаемый результат | 1. 201, код сохранён в верхнем регистре, `usedCount: 0`. 2. Код в списке. Не-админу — 403, без JWT — 401 |
| Постусловия | — |

### TC-PRM-002 — Дубль кода: 409 promoExists
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / негатив. |
| Автотест | unit: «409 promoExists при дубле кода»; int: «409 promoExists на дубль кода (регистронезависимо)»; web: demo-engine.spec «дубль — 409», AdminView.spec «дубль кода — подсказка» |
| Предусловия | Код уже существует |
| Шаги | 1. `POST /api/promos` с тем же кодом в другом регистре |
| Ожидаемый результат | 409, тело содержит `code: promoExists`; список не изменился |
| Постусловия | — |

### TC-PRM-003 — Границы создания: процент ≤ 99, срок в будущем
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / гранич. |
| Автотест | unit: «400 на процент больше 99», «400 на срок действия в прошлом»; int: «400 на кривые данные (percent 150)»; web: demo-engine.spec «процент >99 — 400» |
| Предусловия | Админ авторизован |
| Шаги | 1. kind=percent, value=100+. 2. value=0. 3. expiresAt в прошлом. 4. Код с кириллицей |
| Ожидаемый результат | Все — 400 (ValidationPipe/сервис); граница процента — семантика сервиса (зависит от kind) |
| Постусловия | — |

### TC-PRM-004 — Превью промокода пересчитывает сумму без списания
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | unit: `promo.logic.spec` (процент/округление/кламп); int: «превью процента», «фикс больше суммы клампится»; web: PaymentView.spec «превью: зачёркнутая база…» |
| Предусловия | Своя бронь в PENDING_PAYMENT, код активен |
| Шаги | 1. `POST /api/promos/validate {code, bookingId}` для percent. 2. Для fixed больше суммы. 3. `GET /api/promos` |
| Ожидаемый результат | 200: `discountRub` (процент — с округлением до рубля; фикс — кламп к сумме), `totalRub` = сумма − скидка. `usedCount` не изменился |
| Постусловия | — |

### TC-PRM-005 — Отказы превью: 404/410/409/403
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / негатив. |
| Автотест | int: «404 promoNotFound, 410 promoExpired, 409 promoExhausted», «403 на чужую бронь…»; unit: promos.service.spec; web: PaymentView.spec «неизвестный и истёкший — подсказки» |
| Предусловия | Неизвестный, истёкший и исчерпанный коды; чужая бронь; CONFIRMED-бронь |
| Шаги | 1. validate неизвестного. 2. Истёкшего. 3. Исчерпанного. 4. Чужой брони. 5. Не-PENDING_PAYMENT брони |
| Ожидаемый результат | 1. 404 promoNotFound. 2. 410 promoExpired. 3. 409 promoExhausted. 4. 403. 5. 409 со `status` в теле |
| Постусловия | — |

### TC-PRM-006 — Оплата с промокодом: скидка в totalRub, воркер списывает её
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | unit: «применяет промокод…», «воркеру уходит событие со скидочной суммой»; int: «200: скидка в totalRub…»; e2e «validate → pay с промокодом → воркер подтверждает…»; web: PaymentView.spec «вердикт со скидочной суммой» |
| Предусловия | Бронь в PENDING_PAYMENT, код активен |
| Шаги | 1. `POST /api/bookings/:id/pay {promoCode}`. 2. Дождаться вердикта. 3. `GET /api/promos` |
| Ожидаемый результат | 200: статус PENDING, `totalRub` уменьшен, `promoCode`/`discountRub` на брони; событие `booking.created` воркеру — со скидочной суммой; вердикт по ней же; `usedCount` вырос |
| Постусловия | — |

### TC-PRM-007 — Гонка за последний код: откат транзакции оплаты
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / конкурент. |
| Автотест | unit: «гонка за последний код: активация не прошла → 409, оплата откатилась»; int: «гонка за последний код: 409, бронь осталась payable» (честный ROLLBACK-фейк); e2e «гонка за последний код»; web: PaymentView.spec «код исчерпали между применением и оплатой» |
| Предусловия | У кода осталась последняя активация, две брони в PENDING_PAYMENT |
| Шаги | 1. Оплатить первую бронь кодом (успех). 2. Оплатить вторую тем же кодом |
| Ожидаемый результат | Первая — 200 со скидкой. Вторая — 409 promoExhausted, и транзакция откатилась целиком: бронь осталась в PENDING_PAYMENT (можно оплатить без кода), события воркеру не было |
| Постусловия | — |

### TC-PRM-008 — Демо-паритет промокодов на Pages
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / функц. |
| Автотест | web: demo-engine.spec «промокоды» (превью/ошибки/pay/reset), promos.store.spec, AdminView.spec «демо: промо-секция открыта всем» |
| Предусловия | Демка на GitHub Pages |
| Шаги | 1. Забронировать места, применить `CINE10` на /pay. 2. Ввести `EXPIRED5`. 3. Дважды применить `SUMMER300` (лимит 3, осталось 1). 4. Открыть `/admin` — создать свой код |
| Ожидаемый результат | 1. Превью −10%, кнопка со скидочной суммой, вердикт «воркера» — со скидочной. 2. Подсказка «срок истёк». 3. Первая — ок, вторая — «лимит исчерпан», бронь остаётся оплачиваемой. 4. Код создаётся и работает |
| Постусловия | — |

## Модуль «QR-билеты»

### TC-QR-001 — Выдача билетов по CONFIRMED-брони
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | unit: bookings.service.spec «по билету на каждое место…», «подпись детерминирована: повторная выдача»; int: tickets.int.spec «200: по билету на место, подпись детерминирована»; e2e «CONFIRMED → по билету на место»; web: tickets.store.spec, TicketView.spec «CONFIRMED: карточка на каждое место» |
| Предусловия | Бронь в CONFIRMED на 2+ мест |
| Шаги | 1. `GET /api/bookings/:id/tickets`. 2. Повторить запрос |
| Ожидаемый результат | 1. 200, по билету на каждое место: `signature` hex-128, `ticketNo` TK-XXXXXX, контекст фильма/сеанса/гостя. 2. Те же подписи и номера — билет производная, состояния нет |
| Постусловия | — |

### TC-QR-002 — Не-CONFIRMED бронь: 409 bookingNotConfirmed, сага гасит билеты
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / негатив. |
| Автотест | unit: «409 bookingNotConfirmed: бронь ещё не подтверждена», «…отменённая бронь гасит билеты»; int: «409 bookingNotConfirmed: бронь ещё ждёт оплаты»; e2e «…до подтверждения — 409», «отмена гасит билеты»; web: demo-engine.spec «409 до подтверждения», «сага возврата: CANCELLED гасит билеты, откат оживляет» |
| Предусловия | Брони в PENDING_PAYMENT, CANCELLED; CONFIRMED-бронь перед отменой |
| Шаги | 1. Запросить билеты ждущей брони. 2. Отменённой. 3. CONFIRMED → отмена → дождаться саги → запросить |
| Ожидаемый результат | 1–2. 409 с кодом bookingNotConfirmed и текущим статусом. 3. После CANCELLED — 409; при REFUND_FAILED (откат в CONFIRMED) билеты снова выдаются |
| Постусловия | — |

### TC-QR-003 — Чужому владельцу билеты не выдаются
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / безопасн. |
| Автотест | unit: «403: чужие билеты не выдаются»; int: «403: чужому владельцу»; e2e «403: билеты видит только владелец» |
| Предусловия | Бронь пользователя A, JWT пользователя B |
| Шаги | 1. `GET /api/bookings/:id/tickets` токеном B |
| Ожидаемый результат | 403 «Это не ваша бронь» |
| Постусловия | — |

### TC-QR-004 — Подпись HMAC: канон, детерминизм, место и секрет меняют подпись
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | unit: ticket.logic.spec (api) и ticket.spec (web) — зеркальные векторы, sha256.spec (FIPS/RFC 4231 + сверка с node:crypto) |
| Предусловия | — |
| Шаги | 1. Посчитать подпись канона `CINE1|id|seat|epoch` дважды. 2. Сменить место. 3. Сменить секрет. 4. Сравнить чистую логику web с node:crypto |
| Ожидаемый результат | 1. Одинаковые hex-128. 2–3. Другие подписи. 4. Полное совпадение — демо и live считают одинаково, расходится только секрет |
| Постусловия | — |

### TC-QR-005 — Сканер: честный QR — valid с контекстом
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / функц. |
| Автотест | unit: «валидный билет → true с контекстом»; int: «200 valid»; e2e «сканер: честный QR — valid» |
| Предусловия | CONFIRMED-бронь на будущий сеанс, QR-строка из полей билета, JWT админа |
| Шаги | 1. `POST /api/bookings/tickets/verify {payload}` админом. 2. Тем же токеном обычного пользователя |
| Ожидаемый результат | 1. 200 `{valid: true, reason: null}` + бронь/место/фильм/зал/гость. 2. 403 — сканер только персоналу |
| Постусловия | — |

### TC-QR-006 — Сканер: подделка и мусор отвергаются подписью
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / негатив. |
| Автотест | unit: «подделка подписи → false, badSignature», «мусорная строка → malformedPayload»; int: «200 badSignature», «200 malformedPayload»; e2e «подделка — badSignature»; web: demo-engine.spec «сканер: честный QR — valid, подделка…» |
| Предусловия | Валидный билет; изменённая подпись; строка чужого формата |
| Шаги | 1. verify с перевёрнутой подписью (до БД дело не доходит). 2. verify мусорной строки |
| Ожидаемый результат | 1. `{valid: false, reason: 'badSignature'}` без обращения к БД. 2. `{valid: false, reason: 'malformedPayload'}` |
| Постусловия | — |

### TC-QR-007 — Сканер: бронь/место/сеанс — вердикты с причиной
| Поле | Значение |
|---|---|
| Приоритет / Тип | High / негатив. |
| Автотест | unit: «бронь не найдена/не подтверждена», «чужое место (подпись честная)», «сеанс уже прошёл»; int: «200 bookingNotFound», «…bookingNotConfirmed», «…seatMismatch», «…sessionPassed»; web: demo-engine.spec «чужое место — seatMismatch» |
| Предусловия | Честно подписанные QR: несуществующей брони, отменённой брони, чужого места, прошедшего сеанса |
| Шаги | 1–4. verify каждого payload |
| Ожидаемый результат | Вердикты `bookingNotFound`, `bookingNotConfirmed`, `seatMismatch`, `sessionPassed` — каждый 200 с причиной и контекстом, где он есть |
| Постусловия | — |

### TC-QR-008 — Экран «на вход в зал» и демо-паритет
| Поле | Значение |
|---|---|
| Приоритет / Тип | Medium / функц. |
| Автотест | web: TicketView.spec (карточки по местам, fullscreen+Esc, ошибка до подтверждения), QrCode.spec, demo-engine.spec «QR-билеты», tickets.store.spec |
| Предусловия | Демка на GitHub Pages или стенд |
| Шаги | 1. Оплатить бронь → с /pay перейти «Показать QR-билеты». 2. Кликнуть QR → полноэкранный режим, Esc. 3. Открыть /ticket/:id неоплаченной брони. 4. В демо: отменить подтверждённую и обновить экран |
| Ожидаемый результат | 1. Карточки по местам с TK-номерами и QR. 2. Полноэкранный QR, Esc закрывает. 3. Объяснение про подтверждённую бронь. 4. 409-экран после гашения билетов движком |
| Постусловия | — |

---

## Отчёт о прогоне (шаблон)

```text
Отчёт о прогоне №<N> — <дата>
Ветка/коммит: <ref>
Окружение: docker compose стенд / версия-node / go

Набор                  Пройдено  Упало  Пропущено  Время
web/vitest             43        0      0          2 c
api/unit (jest)        43        0      0          10 c
api/integration        25        0      0          9 c
api/e2e (стенд)        8         0      8*         20 c   * — skipped без стенда

Дефекты: <список или «не найдены»>
Вывод: <годен / годен с замечаниями / не годен>
```

Пример фактического прогона 2026-09-05 (стенд не поднимался, e2e skipped):
web 43/43, api-unit 43/43, integration 25/25, e2e 8 skipped — годен,
e2e требует повтора на стенде до релиза.
